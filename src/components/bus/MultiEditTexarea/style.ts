import styled from '@emotion/styled';

export const MultiEditWrapper = styled.div`
  .textarea {
    margin-top: 5px;
    height: 200px;
    resize: none;
  }

  .footer-text {
    font-size: 12px;
  }
`;
