import { css } from '@emotion/css';

export const defaultColumnWrapper = css`
  width: 100%;

  .ant-checkbox-wrapper {
    margin: 0 5px;
  }
  .txt-title {
    padding-left: 10px;
  }
  .ant-input {
    border: none;
    border-radius: 0;
    background-color: transparent;
  }
  .ant-btn {
    border: none;
    border-radius: 0;
  }
`;
